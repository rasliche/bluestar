extends ../layouts/main-layout

block content
    form(action="/quizzes", method="post").border.text-center
        input(type="hidden", name="_csrf", value=csrfToken)
        div.py-2
            label(for="title").px-2 Quiz Title
            input(
                type="text", 
                id="title", 
                name="title",
                class="border-b-2 px-2 border-gray-light rounded focus:border focus:border-blue")
        //- div.py-2
        //-     label(for="content").px-2 Questions
        //-     input(
        //-         type="text", 
        //-         id="content", 
        //-         name="content",
        //-         class="border-b-2 px-2 border-gray-light rounded focus:border focus:border-blue")
        button(type="submit") Add Quiz