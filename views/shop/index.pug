extends ../layouts/main-layout.pug

block content
    ul
        each shop in shops
            li
                a(href="/shops/"+shop._id) #{shop.name}
                if loggedInUser.isAdmin
                    | (#{shop})
                    | #[a(href="/shops/"+shop._id+"/edit").text-sm Edit]