extends ../layouts/main-layout.pug

block content
    h1= pageTitle
    ul
        each shop in shops
            li #{shop.name}
                if user.isAdmin
                    |  #[a(href="edit/"+shop._id) Edit]